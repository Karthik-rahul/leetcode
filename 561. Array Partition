class Solution {
    public int arrayPairSum(int[] nums) {
        Arrays.sort(nums);
        int sum=0;
        int min,i;
        for(i=0;i<nums.length;i=i+2){
            sum=sum+nums[i];
        }
        return sum;
        
    }
}
